# deployment for Mysql
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: expense
  # Labels of deployment resource
  labels:
    app: mysql
    tier: mysql
    project: expense
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
      tier: mysql
      project: expense
  template:
    metadata:
      labels:
        app: mysql
        tier: mysql
        project: expense
    spec:
      containers:
      - name: mysql
        image: rahul8096/deployment:1.0.0
